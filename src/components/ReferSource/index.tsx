import React from 'react'

const Reference: React.FC<{ pluginList: Array<[string, string]>, inspiration: string }> = ({ pluginList, inspiration }) => {
  return (<div>
    项目灵感来源：URL
    项目使用插件地址
  </div>)
}

export default Reference